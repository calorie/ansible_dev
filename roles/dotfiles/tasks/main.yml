---
- name: Clone dotfiles
  git:
    repo: git@github.com:calorie/dotfiles.git
    dest: ~/dotfiles
    version: master

- name: Setup dotfiles
  shell: 'echo y | ./script/setup'
  args:
    chdir: ~/dotfiles
    creates: ~/.zshrc
